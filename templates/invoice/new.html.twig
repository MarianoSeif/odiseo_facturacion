{% extends 'base.html.twig' %}

{% block body %}
    <h2>New Invoice</h2>

    <div class="content">
        <div class="col-xs-12">
            {{ form_start(invoiceForm, {'attr': {'novalidate': 'novalidate'}}) }}
                {{ form_widget(invoiceForm) }}
                <button class="btn btn-primary btn-lg" type="submit" formmethod="post"><i class="fa fa-save"></i> Create</button>
            {{ form_end(invoiceForm) }}
        </div>
    </div>
{% endblock %}

